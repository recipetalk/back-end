spring:
  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: create
        highlight_sql: true
        format_sql: true
